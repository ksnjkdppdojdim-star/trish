version: '3.9'

services:
  trish:
    build: .
    image: trish:latest
    container_name: trish-terminal
    command: /bin/sh
    stdin_open: true
    tty: true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
